Terminator = CR LF;
ExtraInput = Ignore;
ReplyTimeout = 250;

getIDLE {
	in "waiting %d";
}

getRunJob {
    in "run job %d";
}
getRunStatusSec {
    in "runtime=%d";
}
getExitStatus {
    in "htstream complete rc %s";
}

getExitStatusEnum {
    in "htstream complete rc %{xx|None|-}";
}
