Terminator = CR LF;
ExtraInput = Ignore;
ReplyTimeout = 2500;

getIDLE {
	in "waiting %d\r";
}

getRunJob {
    in "run job %d";
}
getRunStatusSec {
    in "+%d:%*c%*d";
}
getExitStatus {
    in "htstream complete rc %s";
}

getExitStatusEnum {
    in "htstream complete rc %{xx|None|-}";
}
